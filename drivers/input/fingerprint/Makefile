#
# Makefile for the fingerprint drivers.
#

# Each configuration option enables a list of files.

obj-$(CONFIG_FINGERPRINT_FPC_VAYU)	+= fpc_vayu/
obj-$(CONFIG_FINGERPRINT_GOODIX_TA_VAYU)	+= goodix_ta_vayu/
