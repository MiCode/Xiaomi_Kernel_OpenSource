
config MTK_LINEARACCHUB
	bool "linearacchub for MediaTek package"
	default n
	help
	  MTK sensor hub driver for Linear acceleration sensor.
	  If this option is set,
	  it will support
	  linearacchub driver.
