config MTK_LTR303
	bool "ltr303 als sensor for MediaTek package"
	help
	  It support different type sensor in this platform
	  If this option is set, it will support ambient
          and light sensor ltr303
