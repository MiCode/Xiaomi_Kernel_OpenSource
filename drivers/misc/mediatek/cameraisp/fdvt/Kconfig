config MTK_CAMERA_FD_ISP6S
	tristate "[FD] Face Detection"
	depends on MTK_CAMERA_ISP_SUPPORT
	help
	  Face Detection.
	  Enable to detect faces on images.
	  Disable if it is not supported or
	  not wanted.

