config MTK_JPEG
	bool "MTK_JPEG"
	default n
	help
	  Control the jpeg encode driver, which is used to enable jpeg encode
          Its default value is no, would
           be set to yes by RD when needed. It's scenario
           is image capture
