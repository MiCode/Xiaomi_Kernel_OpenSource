config MTK_VDEC_FMT
	bool "mtk vdec formatter"
	help
	  This option enables mtk vdec formatter driver,
	  if yes, it means this platform supports VDEC-FMT(or miniMDP) HW,
	  if no, it means this platform not supports VDEC-FMT(or miniMDP) HW.
