. ${ROOT_DIR}/${KERNEL_DIR}/build.config.gki.aarch64

# Overwrite DEFCONFIG with VENDOR_DEFCONFIG (if set)
if [ -n "${VENDOR_DEFCONFIG}" ]; then
  DEFCONFIG=${VENDOR_DEFCONFIG}
  BUILD_GKI_ARTIFACTS=""
  POST_DEFCONFIG_CMDS=""
  TRIM_NONLISTED_KMI=""
  KMI_SYMBOL_LIST_STRICT_MODE=""
  BUILD_SYSTEM_DLKM=0
  MODULES_LIST=""
  MODULES_ORDER=""
fi
